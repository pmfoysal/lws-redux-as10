import { Fragment } from 'react';
import Head from '../../components/head';

export default function AssignmentSubmit() {
   return (
      <Fragment>
         <Head title={`Assignment Submit - `} desc={`Assignment of ""`} />
         <section>AssignmentSubmit</section>;
      </Fragment>
   );
}
