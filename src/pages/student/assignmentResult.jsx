import { Fragment } from 'react';
import Head from '../../components/head';

export default function AssignmentResult() {
   return (
      <Fragment>
         <Head title={`Assignment Result - `} desc={`Assignment of ""`} />
         <section>AssignmentResult</section>;
      </Fragment>
   );
}
