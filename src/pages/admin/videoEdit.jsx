import { Fragment } from 'react';
import Head from '../../components/head';

export default function VideoEdit() {
   return (
      <Fragment>
         <Head title={`Edit Video - `} desc={``} />
         <section>VideoEdit</section>
      </Fragment>
   );
}
