import { Fragment } from 'react';
import Head from '../../components/head';

export default function QuizEdit() {
   return (
      <Fragment>
         <Head title={`Edit Quiz - `} desc={`Quiz of ""`} />
         <section>QuizEdit</section>
      </Fragment>
   );
}
