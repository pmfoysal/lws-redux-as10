import { Fragment } from 'react';
import Head from '../../components/head';

export default function VideoAdd() {
   return (
      <Fragment>
         <Head title='Add Video' />
         <section>VideoAdd</section>
      </Fragment>
   );
}
